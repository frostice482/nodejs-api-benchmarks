generator client {
	provider = "prisma-client"
	output   = "../src/prisma"
}

datasource db {
	provider = "mysql"
}

model User {
	id String @id
	name String
	email String
	phoneNumber String
	dateOfBirth DateTime
}
